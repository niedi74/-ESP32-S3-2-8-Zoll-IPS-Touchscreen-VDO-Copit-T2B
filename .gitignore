.pio/
.vscode/
*.bin
*.elf

# Lokale WiFi-Zugangsdaten
src/wifi_secret.h
build_err.txt

# Generiert von PlatformIO / ESP-IDF Integration
.dummy/
CMakeLists.txt
dependencies.lock
managed_components/
sdkconfig*

# 3D model binaries — track with Git LFS or keep local only
# Remove these lines once Git LFS is configured for this repo
*.step
*.stp
*.STEP
*.STP
